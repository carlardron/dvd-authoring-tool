<?xml version="1.0" encoding="UTF-8"?>
<subpictures>
  <stream>
    <spu force="yes"
         image="{{buttonNormalPath}}" 
         highlight="{{buttonHighlightPath}}" 
         select="{{buttonSelectPath}}">
    </spu>
  </stream>
</subpictures>